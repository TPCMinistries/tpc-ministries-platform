# Handoff — v2.0 "TPC Alive" execution plan (2026-06-11)

> Written by Claude Code for Codex/ChatGPT to pick up. The milestone is fully defined and committed;
> **no code has been written for v2.0 yet.** Your job: execute Phases 10 → 11 → 12 → 13 in order.
> Full success criteria live in `ROADMAP.md`; requirement definitions in `REQUIREMENTS.md`.
> Read `AGENTS.md` (repo root) for hard rules before touching anything.

## The one date that matters
**Kenya Debrief: Saturday June 27, 9 AM PT / 12 PM ET.** Phase 11 must be live by **~June 20**.
If time gets tight, cut Phase 10 scope (ship DOOR-03 capture unification + skip the rest temporarily) — Phase 11 only depends on the unified capture list, not the nav shrink.

## Reality baseline (from the 2026-06-09 live audit — plan against these numbers)
- 7 auth users (6 admins, 1 real member); 0 sign-ins in 30 days. ~60 member routes exist for that 1 member.
- All engagement/money tables are empty EXCEPT `kenya_trip_participants` (27 rows — the delegates) and `kenya_trip_admin_payments` (9, hand-entered).
- `email_subscribers` is fed by `POST /api/public/newsletter` (live since 2026-06-09). A separate `email_subscriptions` table holds member email prefs.
- A weekly-newsletter cron exists and is enabled — but it reads `email_subscriptions`, NOT `email_subscribers`. Reconciling these two lists is OPS-06 (Phase 12).
- Known security backlog is deliberately deferred to after June 27 (listed in STATE.md) — do not get pulled into it.

## Phase 10 — Surface + Capture Foundation (do first)
Goal: five visible member rooms (Watch, Assess, Pray, Give, Kenya), everything else flag-hidden; every public email capture lands in ONE list with source attribution.

Suggested approach:
1. Add a feature-flag map (e.g. `lib/feature-flags.ts`) listing hidden member routes; enforce in the member layout or middleware with a redirect to the dashboard. Do NOT delete route code (SURF-02).
2. Trim member nav to the five rooms + account settings (SURF-01); sweep admin nav/dashboard for links to hidden routes (SURF-04).
3. Walk each of the five rooms end-to-end against the live schema and fix silent failures (SURF-03). The audit found these rooms mostly work; verify, don't rebuild.
4. DOOR-03: add a `source` column (if missing) to `email_subscribers` and route ALL capture points through one insert path — AI widget, assessment, footer newsletter (`/api/public/newsletter`), debrief registration, giving page. Migration must be additive.
5. DOOR-04: gate the push-notification permission prompt — never on first page load; only after second visit (localStorage flag) or a completed action.

## Phase 11 — Kenya Debrief Activation (hard date — start as soon as 10's capture work lands)
Existing assets to build on (verify before writing new code):
- `app/(public)/kenya-debrief/page.tsx` + `_components/register-form.tsx` — registration page is LIVE
- `app/api/kenya/debrief-calendar/route.ts` — .ics generation exists
- A reminder cron for the debrief was wired ~2026-05 — find it (check `vercel.json` / `app/api/cron/`) and TEST it against real rows (KENYA-03)
- `app/(member)/kenya-trip/` — delegate portal components exist; `app/api/kenya/delegate/route.ts`
- 27 delegates already in `kenya_trip_participants`

Work items:
1. KENYA-01: personal invite emails to the 27 delegates (send from **tpcmin.com**) linking to the delegate portal; trip media gated behind login. Get Lorenzo's explicit go-ahead before the actual send to 27 real people.
2. KENYA-02: confirm registration → confirmation email with .ics works end-to-end (test registration).
3. KENYA-03: verify T-7 / T-1 / day-of reminders fire from the cron. T-7 for June 27 is **June 20** — this is what sets the deadline.
4. KENYA-04: post-debrief CTA into assessment/email-capture, conversions attributed `source='debrief'`.
5. KENYA-05: activation scoreboard in the Kenya Command Center (admin): invites sent, portal logins, registrations, post-event captures.

## Phase 12 — AI Front Door + Ops Foundation
1. DOOR-01: Ask Prophet Lorenzo widget — at message limit, prompt for EMAIL (not account); store with `source='ai_widget'`, let conversation continue.
2. DOOR-02: assessments — email gate for full results, `source='assessment'`, link result row to email.
3. OPS-06: reconcile `email_subscribers` + `email_subscriptions` into one deduplicated send list (a DB view is fine); unsubscribe honored from either side; point the weekly-newsletter cron at it.
4. OPS-05: Monday-morning Telegram digest to Lorenzo (new subscribers, prayer requests, debrief registrations, AI conversations, giving). A Telegram bot already exists in the ecosystem (@LDC_Brain_Bot pattern) — a simple bot-token + chat-id sendMessage from a cron is all that's needed. Telegram bot token: ask Lorenzo or check `~/.secrets/` registry (never commit it).

## Phase 13 — Weekly AI Ops Loop
1. OPS-01/02: weekly cron drafts a devotional (grounded in actual teachings/sermon corpus in the `teachings` table) and a newsletter (from the week's real platform activity). Store as rows in a `content_drafts`-style queue table (additive migration, RLS on).
2. OPS-03: one-tap approve/reject from phone — Telegram inline buttons hitting a signed callback route, or a signed magic-link page. No admin login required.
3. OPS-04: on approval, send via the existing Resend pipeline to the Phase-12 unified list. **Unapproved drafts never send. No fallback auto-send. Ever.**

## Conventions
- Commit style: short imperative subjects, docs committed alongside code (see `git log`).
- Build must pass (`npm run build`) before push; Vercel auto-deploys main.
- Update `.planning/STATE.md` + `REQUIREMENTS.md` checkboxes + `ROADMAP.md` progress table as you complete things — the next agent (or Claude) resumes from those files.
- When in doubt about scope: v2.0 is "operate, don't build." Smallest change that makes the success criterion TRUE.

## Open questions for Lorenzo (don't block on these; ask when relevant)
- Real mailing address for the give-by-check line (placeholder points at info@tpcmin.org).
- Payments decision: one $10 end-to-end test on /giving decides platform-processes vs. logbook (deferred, don't pick it up unprompted).
- Approval channel preference for OPS-03: Telegram buttons vs. magic link (Telegram recommended — digest already lands there).
